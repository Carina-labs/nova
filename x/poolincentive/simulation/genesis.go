package simulation

import "github.com/cosmos/cosmos-sdk/types/module"

// RandomizedGenState generates a random GenesisState for pool-incentive
func RandomizedGenState(simState *module.SimulationState) {
	// TODO : implements this!
}
